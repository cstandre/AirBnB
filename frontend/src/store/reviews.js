import { csrfFetch } from "./csrf";

Get_REVIEWS = 'reviews/getSpotReviews';

const initalState = {};

export default function reviewReducer(state = initalState, action) {
    switch(action.type) {
        default:
            return state;
    }
}
